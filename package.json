{
  "name": "merkle_stratumn",
  "version": "1.0.0",
  "description": "Basic Merkle Tree implementation",
  "main": "index.js",
  "author": "Matthieu Guigon",
  "license": "MIT",
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc-watch --onSuccess \"node ./build/MerkleTree.js\"",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "test": "jest --coverage",
    "test-watch": "jest --watch --coverage"
  },
  "devDependencies": {
    "@types/jest": "^26.0.19",
    "@types/node": "^14.14.12",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "tsc-watch": "^4.2.9",
    "tslint": "^6.1.3",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "crypto": "^1.0.1"
  }
}
